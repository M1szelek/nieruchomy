<%= form_for(@offer_attachment) do |f| %>
  <% if @offer_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer_attachment.errors.count, "error") %> prohibited this offer_attachment from being saved:</h2>

      <ul>
      <% @offer_attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :offer_id %><br>
    <%= f.number_field :offer_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
